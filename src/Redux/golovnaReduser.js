

const initialState = {
    
}

const golovnaReduser = (state = initialState, action) => {
    switch (action.type) {
        case ' ':
            break;
        default:
            return state;
    }
}

export { golovnaReduser };