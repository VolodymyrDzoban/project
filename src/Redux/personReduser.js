
const initialState = {

}

const personReduser = (state = initialState, action) => {
    switch (action.type) {
        case ' ':
            return state;
        default:
            return state;
    }
}

export { personReduser }